<!--logs.wxml-->
<view class="container news">
  <block wx:for='{{ newsList }}' wx:key="{{ index }}">
    <view class='news-item-wrapper'>
      <view class='news-item' data-props="{{ { id: index } }}" bindtap="handleOnNewsItem">
        {{ item }}
      </view>
    </view>
  </block>
</view>
